version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: resualign-db
    environment:
      POSTGRES_USER: resualign
      POSTGRES_PASSWORD: resualign
      POSTGRES_DB: resualign
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U resualign"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: resualign-cache
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  redisdata:
